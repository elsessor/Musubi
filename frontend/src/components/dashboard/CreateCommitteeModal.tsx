"use client";

import { useMemo, useState } from "react";
import { Check, Search, UsersRound, X } from "lucide-react";

import type { OrganizationMember } from "@/services/auth.service";

export function CreateCommitteeModal({ members, onClose, onCreate }: { members: OrganizationMember[]; onClose: () => void; onCreate: (input: { name: string; description: string; headMemberId: string | null; memberIds: string[] }) => Promise<void>; }) {
  const [name, setName] = useState(""); const [description, setDescription] = useState(""); const [headMemberId, setHeadMemberId] = useState<string | null>(null); const [memberIds, setMemberIds] = useState<string[]>([]); const [query, setQuery] = useState(""); const [saving, setSaving] = useState(false); const [error, setError] = useState("");
  const visibleMembers = useMemo(() => members.filter((member) => `${member.name} ${member.position} ${member.role}`.toLowerCase().includes(query.trim().toLowerCase())), [members, query]);
  const toggleMember = (id: string) => setMemberIds((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]);
  async function submit(event: React.FormEvent) { event.preventDefault(); if (!name.trim()) { setError("Enter a committee name."); return; } if (!description.trim()) { setError("Enter a short committee description."); return; } if (!memberIds.length) { setError("Select at least one member."); return; } setSaving(true); setError(""); try { await onCreate({ name: name.trim(), description: description.trim(), headMemberId, memberIds }); } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to create committee."); setSaving(false); } }
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4" role="dialog" aria-modal="true" aria-labelledby="create-committee-title"><form onSubmit={submit} className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white shadow-2xl"><div className="flex items-start justify-between border-b border-slate-200 p-5"><div><p className="text-xs font-bold uppercase tracking-wide text-[#2868ed]">Organization</p><h2 id="create-committee-title" className="mt-1 text-xl font-extrabold text-slate-900">Create committee</h2><p className="mt-1 text-sm text-slate-500">Add a head and the members who will work together.</p></div><button type="button" onClick={onClose} disabled={saving} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100"><X className="size-5" /></button></div><div className="space-y-5 p-5"><label className="block text-sm font-semibold text-slate-700">Committee name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-[#2868ed] focus:ring-1 focus:ring-[#2868ed]" placeholder="e.g. Events Committee" autoFocus /></label><label className="block text-sm font-semibold text-slate-700">Description<textarea value={description} onChange={(event) => setDescription(event.target.value)} className="mt-1.5 min-h-20 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-[#2868ed] focus:ring-1 focus:ring-[#2868ed]" placeholder="What will this committee be responsible for?" /></label><label className="block text-sm font-semibold text-slate-700">Committee head<select value={headMemberId ?? ""} onChange={(event) => { const id = event.target.value || null; setHeadMemberId(id); if (id && !memberIds.includes(id)) setMemberIds((current) => [...current, id]); }} className="mt-1.5 w-full rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-[#2868ed]"><option value="">Choose a head (optional)</option>{members.map((member) => <option key={member.id} value={member.id}>{member.name} — {member.position || member.role}</option>)}</select></label><div><div className="flex items-center justify-between"><label className="text-sm font-semibold text-slate-700">Members <span className="text-slate-400">({memberIds.length})</span></label><UsersRound className="size-4 text-slate-400" /></div><label className="mt-2 flex h-10 items-center gap-2 rounded-xl border border-slate-200 px-3"><Search className="size-4 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent text-sm outline-none" placeholder="Search organization members" /></label><div className="mt-2 max-h-52 divide-y overflow-y-auto rounded-xl border border-slate-200">{visibleMembers.map((member) => { const selected = memberIds.includes(member.id); return <button type="button" onClick={() => toggleMember(member.id)} key={member.id} className="flex w-full items-center gap-3 px-3 py-2.5 text-left hover:bg-slate-50"><span className="flex size-8 items-center justify-center rounded-full bg-[#213f68] text-[10px] font-bold text-white">{member.name.split(/\s+/).map((part) => part[0]).slice(0, 2).join("")}</span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-semibold text-slate-800">{member.name}</span><span className="block truncate text-xs text-slate-500">{member.position || member.role}</span></span><span className={`flex size-5 items-center justify-center rounded-md border ${selected ? "border-[#2868ed] bg-[#2868ed] text-white" : "border-slate-300"}`}>{selected ? <Check className="size-3.5" /> : null}</span></button>; })}{!visibleMembers.length ? <p className="p-5 text-center text-sm text-slate-500">No matching members.</p> : null}</div></div>{error ? <p className="rounded-xl bg-rose-50 px-3 py-2 text-sm text-rose-700">{error}</p> : null}</div><div className="flex justify-end gap-3 border-t border-slate-200 p-5"><button type="button" onClick={onClose} disabled={saving} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600">Cancel</button><button type="submit" disabled={saving} className="rounded-xl bg-[#213f68] px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{saving ? "Creating..." : "Create committee"}</button></div></form></div>;
}
